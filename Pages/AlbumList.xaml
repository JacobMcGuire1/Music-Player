<Page
    x:Class="Music_thing.AlbumList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Music_thing"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <GridView ItemsSource="{x:Bind Albums}" x:Name="jess" CanDragItems="True">
            <GridView.ItemTemplate>
                <DataTemplate x:Name="IconTextTemplate" x:DataType="local:Album">
                    
                    <Button x:Name="albumbutton" Click="Albumbutton_Click" Tag="{x:Bind key}" Background="Transparent">
                            <!--Need to add play and add to playlist buttons-->
                        <StackPanel Orientation="Vertical" Margin="0,0,0,0" HorizontalAlignment="Stretch" MinHeight="275" Height="Auto" CanDrag="True" DragStarting="StackPanel_DragStarting" Drop="StackPanel_Drop" Tag="{x:Bind key}">
                                <Image Width="200" Height="200" Source="{x:Bind Albumart, Mode=OneWay}"/>
                                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                    <Button x:Name="playalbumButton" Click="playalbumButton_Click"  FontFamily="Segoe MDL2 Assets" Content="&#xE768;" Width="44" Height="44" Background="Transparent" Margin="0,0,5,0" Tag="{x:Bind key}"/>
                                    <StackPanel Orientation="Vertical">
                                        <TextBlock Text="{x:Bind name}" VerticalAlignment="Center" FontSize="15" Margin="0,0,5,0" Width="150" TextWrapping="WrapWholeWords"/>
                                        <TextBlock Text="{x:Bind artist}" VerticalAlignment="Center" FontSize="13" Margin="0,0,5,0" Width="150" TextWrapping="WrapWholeWords"/>
                                        <TextBlock Text="{x:Bind GetStringYear()}" VerticalAlignment="Center" FontSize="13" Margin="0,0,5,0" Width="150" TextWrapping="WrapWholeWords"/>
                                    </StackPanel>
                                </StackPanel>




                            </StackPanel>
                    </Button>
                    
                </DataTemplate>
            </GridView.ItemTemplate>
            <GridView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid  Orientation="Horizontal" HorizontalAlignment="Center"/>
                </ItemsPanelTemplate>
            </GridView.ItemsPanel>
        </GridView>



    </Grid>
</Page>
