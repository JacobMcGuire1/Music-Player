<Page
    x:Class="Music_thing.ArtistList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Music_thing"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    NavigationCacheMode="Enabled"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <ListView x:Name="ListViewArtists" Margin="20,20,20,0" ItemsSource="{x:Bind Artists}" ItemClick="ListViewArtists_ItemClick" IsItemClickEnabled="True">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:Artist">
                    <StackPanel Orientation="Horizontal" Height="44" Margin="0,0,0,0" Tag="{x:Bind name}">
                        <TextBlock Text="{x:Bind name}" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <TextBlock Text=" - " VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <TextBlock Text="{x:Bind GetStringAlbumCount()}" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</Page>
