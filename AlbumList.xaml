<Page
    x:Class="Music_thing.AlbumList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Music_thing"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <ListView x:Name="ListViewArtists" Margin="20,20,20,0" ItemsSource="{x:Bind Albums}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:Album">
                    <Button x:Name="albumbutton" Click="Albumbutton_Click" Tag="{x:Bind key}">
                        <!--Need to add play and add to playlist buttons-->
                        <StackPanel Orientation="Horizontal" Height="44" Margin="0,0,0,0">
                            <TextBlock Text="{x:Bind name}" VerticalAlignment="Center" Width="400" Margin="0,0,5,0"/>
                            <TextBlock Text="{x:Bind artist}" VerticalAlignment="Center" Width="250" Margin="0,0,5,0"/>
                            <TextBlock Text="{x:Bind key}" VerticalAlignment="Center" Width="250" Margin="0,0,5,0"/>
                        </StackPanel>
                    </Button>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>



    </Grid>
</Page>
